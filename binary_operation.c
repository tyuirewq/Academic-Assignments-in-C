/******************************************************************************

                            Online C Compiler.
                Code, Compile, Run and Debug C program online.
Write your code in this editor and press "Run" button to compile and execute it.

*******************************************************************************/

#include <stdio.h>
    
    int main()
    {   int P = 0x2F39; 
    if ((P== 0xE) ||(P== 0xD ))|| ((P==0x7)||(P==0x8))
       { printf("P is %d ", P);
        printf("Yes there are at least 3 1s in the LSH digit");
       }
    else 
       printf("ther is no 3 1s in ls LSh digit");
        return 0;
    }
    // 