/******************************************************************************

Welcome to GDB Online.
GDB online is an online compiler and debugger tool for C, C++, Python, PHP, Ruby, 
C#, VB, Perl, Swift, Prolog, Javascript, Pascal, HTML, CSS, JS
Code, Compile, Run and Debug online from anywhere in world.

*******************************************************************************/
#include <stdio.h>

int
main ()
{
/*  int a = 011100, d = 450000;
  int leftsht, c, anD, orop, xorop , h;

  leftsht = a << 2;
  anD = a & d; // it is the and operation 
  orop = a | d;  // it is the or operation
  xorop = a ^ d; // it is the xor operation which can be used to check the equality condition
  h = ~a; // it is the not operation
  c = a >> 3;
  printf ("leftsht is : %d \nAndop is : %d \nleftsht is : %d \nf is : %d\nxorop is : %d\nh is : %d", leftsht, anD,orop, xorop , h);
  1011101  = only first number value is become negative in not operation and rest value is as it is. 
              for this -128 + 32 + 16 + 8 + 4 + 1 == - 67 */
              
  long int c=0xCAFE;
  int a,b;
  float d = 56.253;
  printf("%o",d);
     
  
  
//   a = c&0xF;
//   printf("%x",a);
  
//   a = a<<12;
//   printf("%x",a);
//   b >> 4;
//   printf("%x",b);
   
  return 0;
}


